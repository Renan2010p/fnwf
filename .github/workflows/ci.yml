name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-linux-x86_64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Get version
        id: version
        run: |
          VER=$(grep "version" meson.build | head -1 | sed "s/.*'\(.*\)'.*/\1/")
          SHORT=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "tag=v${VER}-dev+${SHORT}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq meson ninja-build g++ pkg-config \
            libgl1-mesa-dev libasound2-dev libpulse-dev libdbus-1-dev \
            libudev-dev libpipewire-0.3-dev libdecor-0-dev

      - name: Build (SDL2 from source)
        run: |
          meson setup build --buildtype=release
          meson compile -C build

      - name: Package
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          RELEASE="fnwf-classic-${TAG}-linux-x86_64"
          mkdir -p "release/${RELEASE}"
          cp build/fnwf "release/${RELEASE}/"
          cp -r assets "release/${RELEASE}/"
          chmod +x "release/${RELEASE}/fnwf"
          cd release && tar czf "${RELEASE}.tar.gz" "${RELEASE}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fnwf-linux-x86_64
          path: release/fnwf-classic-*-linux-x86_64.tar.gz

  build-windows-x86_64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Get version
        id: version
        shell: bash
        run: |
          VER=$(grep "version" meson.build | head -1 | sed "s/.*'\(.*\)'.*/\1/")
          SHORT=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "tag=v${VER}-dev+${SHORT}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-pkg-config
            mingw-w64-x86_64-meson
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-libgl
            git zip

      - name: Build (SDL2 from source)
        shell: msys2 {0}
        run: |
          meson setup build --buildtype=release
          meson compile -C build

      - name: Package
        shell: msys2 {0}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          RELEASE="fnwf-classic-${TAG}-windows-x86_64"
          mkdir -p "release/${RELEASE}"
          cp build/fnwf.exe "release/${RELEASE}/"
          cp -r assets "release/${RELEASE}/"
          cd release && tar czf "${RELEASE}.tar.gz" "${RELEASE}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fnwf-windows-x86_64
          path: release/fnwf-classic-*-windows-x86_64.tar.gz
