// Platform.cpp — intentionally empty.
// Concrete implementations live in src/platform/<backend>/
// (e.g. src/platform/sdl2/plat_sdl2.cpp)
//
// The factory functions create_platform() / destroy_platform() are defined
// by whichever backend is compiled into the build.

#include "core/Platform.hpp"
