// InputManager.cpp — intentionally empty.
// Concrete implementations live in src/platform/<backend>/
// (e.g. src/platform/sdl2/plat_input.cpp)
//
// The factory functions create_input_manager() / destroy_input_manager()
// are defined by whichever backend is compiled into the build.

#include "core/InputManager.hpp"
