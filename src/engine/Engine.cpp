// Engine.cpp — intentionally empty.
// Concrete implementations live in src/platform/<backend>/
// (e.g. src/platform/sdl2/EngineSDL2.cpp)
//
// The factory functions create_engine() / destroy_engine() are defined
// by whichever backend is compiled into the build.

#include "engine/Engine.hpp"
